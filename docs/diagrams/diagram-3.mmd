flowchart TD
    Q(["Ask CaseAssist"]) --> AUTH["Authorize<br/>role + assigned case"]
    AUTH --> REDACT["Redact PII<br/>SSN, name, contact removed"]
    REDACT --> GROUND["Ground in approved policy<br/>via AI Search"]
    GROUND --> GEN["Generate draft<br/>OpenAI · structured JSON"]
    GEN --> CHECK{"Check the output<br/>citations · PII · safety · decision language"}
    CHECK -->|"clean"| DRAFT(["Draft ready for caseworker"])
    CHECK -->|"flagged"| HUMAN(["Route to a separate human reviewer"])
    DRAFT --> AUDIT["Every step audited"]
    HUMAN --> AUDIT
